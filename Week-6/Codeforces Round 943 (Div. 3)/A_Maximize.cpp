#include<bits/stdc++.h>
using namespace std;

// int gcd(int a, int b) 
// { 
//     int result = min(a, b); 
//     while (result > 0) { 
//         if (a % result == 0 && b % result == 0) { 
//             break; 
//         } 
//         result--; 
//     } 

//     return result; 
// } 

int main(){

    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while(t--)
    {
        int x;
        cin >> x;

       cout << x-1 << '\n';
    }

    return 0;
}
